# Canary fixture

Minimal Python file for routing smoke test. Used by the `canary-001` task to verify that the
router classifies a simple analysis request into the `interactive` lane and exits via `reporter`
within at most two loops.
