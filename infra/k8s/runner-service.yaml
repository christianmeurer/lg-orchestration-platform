apiVersion: v1
kind: Service
metadata:
  name: lula-runner
  namespace: lula-orch
spec:
  # LoadBalancer: App Platform uses the external runner URL via LG_RUNNER_BASE_URL.
  type: LoadBalancer
  selector:
    app: lula-runner
  ports:
    - name: runner-http
      port: 8088
      targetPort: 8088
