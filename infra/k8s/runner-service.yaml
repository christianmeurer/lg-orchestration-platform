apiVersion: v1
kind: Service
metadata:
  name: lula-runner
  namespace: lula-orch
spec:
  # ClusterIP: runner is internal-only; access via orchestrator pod only
  type: ClusterIP
  selector:
    app: lula-runner
  ports:
    - name: runner-http
      port: 8088
      targetPort: 8088
